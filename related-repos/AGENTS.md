# 参考仓库目录规则

- 这里只保存 Git submodule，不保存手动复制的第三方仓库。
- 默认只读分析：不修改 tracked 文件，不在子模块中提交或推送。
- 新增子模块、更换固定 commit 或删除子模块必须由用户明确要求。
- 学习过程中产生的疑问和探索记录写入 `../draft/`，正式结论或文章写入 `../output/`。
- 引用代码时记录子模块名、相对路径、关键符号和当前 commit。
