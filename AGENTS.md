# 仓库级协作指南

## 定位

- 这是一个人与 AI 结对学习的仓库。
- 默认采用教练式但不僵化的协作方式：帮助用户建立心智模型、验证理解并保留探索过程。
- 用户明确要求完整答案或产物时，直接提供，不为制造“教学感”而故意保留关键信息。

## 沟通与知识准则

- 默认使用中文沟通和写作；专业术语第一次出现时保留英文原文。
- 明确区分可验证事实、AI 推断和未验证结论。不把 AI 对用户困惑或动机的推测写成用户的确定想法。
- 引用本地资料时记录相对路径；引用参考仓库时记录仓库名、相对路径、关键符号和 commit；引用网页时保留原始 URL。
- 不把聊天寒暄、工具执行日志、重复表达和无关过程写入学习材料。

## 目录边界

- `.agents/`：只存放可复用的 AI Skill，不存放学习笔记、对话草稿或文章。
- `CLAUDE.md`（含各子目录下同名文件）与 `.claude/skills/*` 是符号链接，分别
  指向同目录的 `AGENTS.md` 与 `.agents/skills/*`，仅用于让 Claude Code 自动
  发现既有约定和技能。`AGENTS.md` 与 `.agents/skills/` 依然是唯一信源，不要
  改动这些符号链接的指向，也不要在 `.claude/` 下另写内容。
- `origin-docs/`：存放原始资料及其忠实翻译，不存放摘要、批注、学习笔记或观点。
- `related-repos/`：只以 Git submodule 存放只读参考仓库。
- `draft/`：存放从学习对话中提炼的中间过程，不是可直接发布的正式文章。
- `output/`：存放经过进一步组织的正式文章或其他明确完成的学习成果。

## 持久化规则

- 普通问答只保留在对话中。只有用户明确调用相应 Skill、要求保存，或任务本身明确要求文件产物时才创建文件。
- 不自动将 `draft/` 中的中间稿提升为 `output/` 中的正式产物。
- 生成正式文章后不自动删除、覆盖或回写对应的学习草稿。
- 创建或修改文件前检查现有内容和未提交改动；完成后执行与产物风险相匹配的验证。
